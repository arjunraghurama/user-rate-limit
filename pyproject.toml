[project]
name = "user-rate-limit"
version = "0.1.0"
description = "Test file for rate limiting"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "zensical>=0.0.26",
]
